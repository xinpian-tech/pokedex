// Auto-generated by gen_do_execute.py

ExecResult do_execute_c(CoreModel& core, CInst inst, MemCallback mem, NextPc& npc) {
  constexpr bool ext_rv32 = CoreModel::XLEN == 32;
  constexpr bool ext_rv64 = CoreModel::XLEN == 64;
  constexpr bool ext_c = true;
  constexpr bool ext_f = CoreModel::FLEN >= 32;
  constexpr bool ext_d = CoreModel::FLEN >= 64;

  // inst group: Zca
  if constexpr (ext_c) {
    if (inst.match(0xe003, 0x0000)) {
      // c.addi4spn
      return EXECUTE_C_ADDI4SPN(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x4000)) {
      // c.lw
      return EXECUTE_C_LW(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xc000)) {
      // c.sw
      return EXECUTE_C_SW(core, inst, mem, npc);
    }
    if (inst.match(0xef83, 0x0001)) {
      // c.nop
      return EXECUTE_C_NOP(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x0001)) {
      // c.addi
      return EXECUTE_C_ADDI(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x4001)) {
      // c.li
      return EXECUTE_C_LI(core, inst, mem, npc);
    }
    if (inst.match(0xef83, 0x6101)) {
      // c.addi16sp
      return EXECUTE_C_ADDI16SP(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x6001)) {
      // c.lui
      return EXECUTE_C_LUI(core, inst, mem, npc);
    }
    if (inst.match(0xec03, 0x8801)) {
      // c.andi
      return EXECUTE_C_ANDI(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x8c01)) {
      // c.sub
      return EXECUTE_C_SUB(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x8c21)) {
      // c.xor
      return EXECUTE_C_XOR(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x8c41)) {
      // c.or
      return EXECUTE_C_OR(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x8c61)) {
      // c.and
      return EXECUTE_C_AND(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xa001)) {
      // c.j
      return EXECUTE_C_J(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xc001)) {
      // c.beqz
      return EXECUTE_C_BEQZ(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xe001)) {
      // c.bnez
      return EXECUTE_C_BNEZ(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x4002)) {
      // c.lwsp
      return EXECUTE_C_LWSP(core, inst, mem, npc);
    }
    if (inst.match(0xf07f, 0x8002)) {
      // c.jr
      return EXECUTE_C_JR(core, inst, mem, npc);
    }
    if (inst.match(0xf003, 0x8002)) {
      // c.mv
      return EXECUTE_C_MV(core, inst, mem, npc);
    }
    if (inst.match(0xffff, 0x9002)) {
      // c.ebreak
      return EXECUTE_C_EBREAK(core, inst, mem, npc);
    }
    if (inst.match(0xf07f, 0x9002)) {
      // c.jalr
      return EXECUTE_C_JALR(core, inst, mem, npc);
    }
    if (inst.match(0xf003, 0x9002)) {
      // c.add
      return EXECUTE_C_ADD(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xc002)) {
      // c.swsp
      return EXECUTE_C_SWSP(core, inst, mem, npc);
    }
  }

  // inst group: Zca_rv32
  if constexpr (ext_rv32 && ext_c) {
    if (inst.match(0xe003, 0x2001)) {
      // c.jal
      return EXECUTE_C_JAL(core, inst, mem, npc);
    }
    if (inst.match(0xfc03, 0x8001)) {
      // c.srli
      return EXECUTE_C_SRLI(core, inst, mem, npc);
    }
    if (inst.match(0xfc03, 0x8401)) {
      // c.srai
      return EXECUTE_C_SRAI(core, inst, mem, npc);
    }
    if (inst.match(0xf003, 0x0002)) {
      // c.slli
      return EXECUTE_C_SLLI(core, inst, mem, npc);
    }
  }

  // inst group: Zca_rv64
  if constexpr (ext_rv64 && ext_c) {
    if (inst.match(0xe003, 0x6000)) {
      // c.ld
      return EXECUTE_C_LD(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xe000)) {
      // c.sd
      return EXECUTE_C_SD(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x2001)) {
      // c.addiw
      return EXECUTE_C_ADDIW(core, inst, mem, npc);
    }
    if (inst.match(0xec03, 0x8001)) {
      // c.srli
      return EXECUTE_C_SRLI(core, inst, mem, npc);
    }
    if (inst.match(0xec03, 0x8401)) {
      // c.srai
      return EXECUTE_C_SRAI(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x9c01)) {
      // c.subw
      return EXECUTE_C_SUBW(core, inst, mem, npc);
    }
    if (inst.match(0xfc63, 0x9c21)) {
      // c.addw
      return EXECUTE_C_ADDW(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x0002)) {
      // c.slli
      return EXECUTE_C_SLLI(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x6002)) {
      // c.ldsp
      return EXECUTE_C_LDSP(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xe002)) {
      // c.sdsp
      return EXECUTE_C_SDSP(core, inst, mem, npc);
    }
  }

  // inst group: Zcf
  if constexpr (ext_rv32 && ext_c && ext_f) {
    if (inst.match(0xe003, 0x6000)) {
      // c.flw
      return EXECUTE_C_FLW(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xe000)) {
      // c.fsw
      return EXECUTE_C_FSW(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x6002)) {
      // c.flwsp
      return EXECUTE_C_FLWSP(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xe002)) {
      // c.fswsp
      return EXECUTE_C_FSWSP(core, inst, mem, npc);
    }
  }

  // inst group: Zcd
  if constexpr (ext_c && ext_d) {
    if (inst.match(0xe003, 0x2000)) {
      // c.fld
      return EXECUTE_C_FLD(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xa000)) {
      // c.fsd
      return EXECUTE_C_FSD(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0x2002)) {
      // c.fldsp
      return EXECUTE_C_FLDSP(core, inst, mem, npc);
    }
    if (inst.match(0xe003, 0xa002)) {
      // c.fsdsp
      return EXECUTE_C_FSDSP(core, inst, mem, npc);
    }
  }

  return ExecResult::illegal_inst();
}
